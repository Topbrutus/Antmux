# Genesis V0.1 — PUBLIC MINIMAL

Date: 2026-09-06
Status before final launch tag: `RC_CANDIDATE`

Public observation URL:

`https://antmux.com/laboratoire/genesis/`

## What this public release is

Genesis V0.1 is a **first public reproducible seed with its state, limits and proofs**.

It is a read-only public observation surface. It does not expose the private Seed Genesis repository, private GESIS data, Memoria, credentials, machine-local paths or write capabilities.

Current public data mode validated before RC:

- `LIVE_READ_ONLY`
- source `PUBLIC_READ_ONLY`
- integrity `VERIFIED_PUBLIC`
- public gate `LIVE_READ_ONLY_ACTIVE`
- snapshot fallback retained

## Scientific boundary

The public release keeps explicit claim classes and failure/inconclusive states.

C080:
- blocked before scientific decode;
- zero scientific decodes;
- no scientific verdict.

C081:
- `VALIDATED_MEASURED_INCONCLUSIVE`;
- Firefox: 18 decodes;
- Chrome: 0 decodes;
- primary hypothesis not tested;
- prediction verdict: none.

C081 therefore neither confirms nor refutes its preregistered prediction.

`C082_STARTED=false` for this release process.

## What V0.1 does not claim

V0.1 does not claim that Genesis is finished, that a browser is universally correct, that an exact internal browser resampling mechanism has been proven, that the observations generalize to arbitrary browsers/OS/versions/machines, or that a new physical phenomenon, consciousness or material has been established.

Scientific rule:

`MEASURED != DERIVED != INTERPRETED != HYPOTHESIS != UNKNOWN`

## Public trust boundary

`PRIVATE GENESIS -> PUBLIC ADAPTER/BRIDGE -> READ-ONLY PUBLIC PAYLOAD -> GENESIS VISION CENTER`

Policy:

`DENY BY DEFAULT -> EXPLICIT WHITELIST -> PUBLIC`

The browser has no public write path back to Seed Genesis, GESIS or Memoria.

## Historical README note

`README.md` contains the chronological LAB-004 construction notes for DEMO, SNAPSHOT and LIVE_READ_ONLY. Some older PENDING labels in those historical sections describe earlier development gates and are superseded by this V0.1 release note and by the currently deployed validated public state.

This document does not create new scientific evidence. It documents the public release boundary only.
