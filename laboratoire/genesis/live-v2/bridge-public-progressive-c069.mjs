#!/usr/bin/env node
import { createHash } from 'node:crypto';
import { mkdir, readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { validatePublicV2 } from '../validate-public-v2.mjs';
import { buildProgressivePublicEnvelopeC068 } from './bridge-public-progressive-c068.mjs';
import { assertProgressiveBridgeInputC069, c069ToExactC068Text } from './build-public-source-progressive-c069.mjs';
import { extractProgressiveGenesisStatusC068, buildProgressiveBridgeInputC068 } from './build-public-source-progressive-c068.mjs';
function fail(m){throw new Error(m);}function stable(v){if(Array.isArray(v))return v.map(stable);if(v&&typeof v==='object')return Object.fromEntries(Object.keys(v).sort().map(k=>[k,stable(v[k])]));return v;}function digestPayload(payload,at){return createHash('sha256').update(JSON.stringify(stable({observed_at:at,payload})),'utf8').digest('hex');}function metric(id,label,value){return{id,label,value,status:'VERIFIED_PUBLIC',provenance_ref:'SERVER-SIDE-WHITELIST-V5-SIGNAL'};}function byId(m,id){const x=m.find(v=>v.id===id);if(!x)fail(`Metric manquante: ${id}`);return x;}
function scanPrivate(value){const text=JSON.stringify(value);const forbidden=['MiniMaxAI/MiniMax-Music3','LOCAL_HUGGINGFACE_DIFFUSERS','Topbrutus/seedgenesis','Topbrutus/gesis','refs/heads/','public/live-source','public/live/status.env','generator_provider','model_name','model_version_or_build','prompt_hash','prompt_bytes','b4992dbc19fcd7f3c81d72c9d9c4662dd0b15139','0b4130568c279807f55b7174ab671812f7a1c905','4df4eaad4b0f400e382936bd500d2b7dbe8bf843','d737d3fa761ccc7112d84387d34a24a3bf8aff5bdb8469e3a436f9b3793ab056','90fba217655edf5ddb7fdcb9394562985ec11233ab4cc1a732c20ef50d933a91','27036b8a14e32c1515df70380bf437bd438f6158857c6094ffcf6490a78d4481','932c41a38dc8cce2e7cccee436f26a200d269c058e69e5de9ec09105013407aa','6ca8caa0b84e685c86d2552d27ae87517e6b8a285857ca1da047aabf5ffc63d7','6c8256a5fcb7a11b44f519880eb792f936f44f49'];for(const x of forbidden)if(text.includes(x))fail(`Donnée privée interdite: ${x}`);}
export function buildProgressivePublicEnvelopeC069(input,options={}){if(input?.publication_intent!=='SERVER_SIDE_PUBLIC_READ_ONLY_PROGRESSIVE_BRIDGE_C069_SIGNAL')return buildProgressivePublicEnvelopeC068(input,options);assertProgressiveBridgeInputC069(input);const found=new Map(Object.entries(input.source_attestation.public_status));const c068=extractProgressiveGenesisStatusC068(c069ToExactC068Text(found));const baseInput=buildProgressiveBridgeInputC068(c068,{now:input.bridge_received_at,liveActive:input.live_active});baseInput.source_observed_at=input.source_observed_at;baseInput.bridge_received_at=input.bridge_received_at;baseInput.max_age_seconds=input.max_age_seconds;const result=buildProgressivePublicEnvelopeC068(baseInput,options);const envelope=structuredClone(result.envelope),metrics=envelope.payload.metrics;byId(metrics,'genesis003-validated-through').value='C069';byId(metrics,'next-scientific-action').value='FREEZE_ANALYSIS_THRESHOLDS_AND_DECISION_RULE';metrics.splice(metrics.findIndex(x=>x.id==='hypothesis-selection'),0,metric('genesis003-c069','C069 · reproductibilité signal → GESIS','VALIDATED_10_OF_10'),metric('signal-gesis-reproducibility-validated','Reproductibilité signal → GESIS validée',true),metric('cross-runner-reproducibility-proven','Reproductibilité inter-runner validée',true),metric('calibration-control-passed','Contrôle de calibration validé',true),metric('provider-model-kernel-dependency','Dépendance provider/modèle du noyau',false));envelope.payload.identity.root_version='GENESIS-003-C069-SIGNAL-PUBLIC-PROGRESS-v5';envelope.payload.evidence=envelope.payload.evidence.filter(x=>x.id!=='LIVE-PUBLIC-PROJECTION-HASH');envelope.payload.evidence[0]={id:'SERVER-SIDE-WHITELIST-V5-SIGNAL',type:'PUBLIC_ATTESTATION',status:'VERIFIED_PUBLIC',public_ref:'Whitelisted corrected C069 signal/GESIS public status'};const digest=digestPayload(envelope.payload,input.source_observed_at);envelope.payload.identity.root_digest=`PUBLIC-READ-ONLY-SHA256:${digest}`;envelope.payload.evidence.push({id:'LIVE-PUBLIC-PROJECTION-HASH',type:'PUBLIC_HASH',status:'VERIFIED_PUBLIC',public_ref:'Server-side progressive public read-only projection',hash:`sha256:${digest}`});envelope.publication_id='GENESIS-PUBLIC-READ-ONLY-PROGRESSIVE-0005-SIGNAL';validatePublicV2(envelope);scanPrivate(envelope);return{envelope,sourceAgeSeconds:result.sourceAgeSeconds};}
async function main(){const p=process.argv[2];if(!p)fail('Usage: bridge-public-progressive-c069.mjs <bridge-input.json>');const input=JSON.parse(await readFile(path.resolve(p),'utf8'));const r=buildProgressivePublicEnvelopeC069(input);const out=path.resolve('.build/genesis-public-read-only-bridge');await mkdir(out,{recursive:true});await writeFile(path.join(out,'public-read-only-envelope.json'),`${JSON.stringify(r.envelope,null,2)}\n`,'utf8');console.log('GENESIS_PROGRESSIVE_C069_SIGNAL_PUBLIC_BRIDGE_VALID');}
if(process.argv[1]===fileURLToPath(import.meta.url))main().catch(e=>{console.error(`GENESIS_PROGRESSIVE_C069_SIGNAL_PUBLIC_BRIDGE_INVALID: ${e.message}`);process.exit(1);});
