# X72 — TEN IMAGES TEST MAP v0.1

Status: **CANDIDATE / TEST PLAN**

The ten supplied images are treated as prompts for possible mathematical/software mechanisms, not as evidence that the pictured claims explain X72. Every imported idea must survive an isolated test before it can influence architecture.

## Stage 1 — Bayesian-style evidence update — IMPLEMENTED

Use a Beta(1,1) ledger per explicit invariant rather than a single yes/no verdict. Current criteria: same source, same tick, opposite stereo angle, left/right reconstruction, energy identity, and gate verification. Repeated passes raise the posterior mean; a contradiction lowers only the criterion it contradicts.

This measures **software repeatability**, not truth about nature.

## Stage 2 — Path ensemble — NEXT CANDIDATE

Inspired by the path-integral image only at the algorithmic level: define several legal routes between known nodes, run them under identical inputs, and compare their measurable outputs. Do not copy quantum amplitudes or Planck constants unless an independent model justifies them.

Exit criterion: route identity, ordering, and result must be reproducible under a fixed seed and tick.

## Stage 3 — Bounded oscillator modes

Use the harmonic-oscillator image as a prompt to test whether bounded periodic modes are useful for internal state transitions. Start with ordinary discrete oscillators; no quantum interpretation is assumed.

Exit criterion: boundedness, deterministic phase, and stable mode transitions.

## Stage 4 — Coupled-field analogy

The electromagnetism images suggest testing paired/orthogonal channels and local propagation rules. Treat these as software fields only. No electromagnetic claim is made.

Exit criterion: conservation/accounting rules, causal update order, and no hidden mutation of Queen state.

## Stage 5 — Geometry of the graph

The Riemann image suggests measuring path length, local distortion, and graph curvature-like quantities on the candidate topology. Begin with graph metrics before considering continuous manifolds.

Exit criterion: metric definitions are explicit, invariant under node relabeling where appropriate, and useful for distinguishing routes.

## Stage 6 — Finite state / expanding path space

The Gabriel's Horn image is used only as an analogy for testing whether a bounded state representation can support a rapidly growing route space. This is a combinatorics/capacity question, not the mathematical Gabriel's Horn itself.

Exit criterion: memory and runtime remain bounded under a stated route-growth policy.

## Supporting prompts

- Slow-light image -> latency / delay / phase experiments only; no claim of manipulating physical light.
- Basic-formulas image -> dimensional and unit sanity checks where physical quantities are ever introduced.
- Scientist collage -> reminder to keep multiple methods available; it is not a mathematical module.

## Discipline

Only LEFT/RIGHT and the reversible Da'at center are currently mapped. Vertical axis, depth axis, full Tree paths, sefirah semantics, and physical interpretations remain explicitly UNMAPPED until a separate experiment justifies them.
